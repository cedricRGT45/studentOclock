import { createSlice } from '@reduxjs/toolkit';

import type {RootState} from '../../index.ts';

export const themeSlice = createSlive({
    name : 'theme',
    valeur_initiale,
    reducers:{
        méthodes pour modifs
    }
})
