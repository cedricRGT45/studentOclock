#La première insttruction doti être form
#ixi on precise l'image de base sur la quelle construire notre ipmage perso

#ici quand je base mon image sur node:latest, je pars d'un conteneur dans lequelle sera installé node js et npm
FROM node:latest

#ensuite on spécifie le répertoire depuis lequel nous allons éxecuter nos commandes
WORKDIR /home/app

#nous allons éxecuter une première commande avec l'instruction "run"
#ici on installe la dépendance "json server"
RUN npm i -g json-server

#on utilise copy pour copier un ficjer ou dossier depuis la machine hôte vers l'image du conteneur
#ici on aura besoin des données .json donc on copie le fichier dans le dossier de l'image
COPY data.json /home/app/data.json

#derniere instruction CMD or ENTRYPOINT
#Ces instruction indiquent quelles commandes utiliser lors du démarrage du conteneur
# !! une seule instruction CMD ou ENTRYPOINT par dockerfile
CMD json-server data.json --host 0.0.0.0

# === "bin/sh json-server data.json --host 0.0.0.0"
#Pour t